/**
 * Interface for classes that represent a DashboardApplication.
 *
 * @interface DashboardApplication.
 */
export interface DashboardApplication {
  imageUrl: string;
  name: string;
  redirectUrl: string;
}
